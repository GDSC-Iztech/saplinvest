import 'package:flutter/material.dart';

const greenBackground = BoxDecoration(
  gradient: LinearGradient(
      colors: [Color(0xFF68BB5A), Color(0xFF17842F)],
      begin: Alignment.topCenter,
      end: Alignment.bottomCenter),
);
